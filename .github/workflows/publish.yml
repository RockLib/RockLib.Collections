name: Publish

on:
  workflow_call:

jobs:
  publish:
    
    runs-on: windows-latest
  
    steps:
      - uses: actions/checkout@v3
      - name: Package #Double back here.
        run: |
          echo "Successfully called workflow" #Test message
 #       run: dotnet pack RockLib.Collections/RockLib.Collections.csproj --configuration Release #--no-build -o "./"
 # 
 #     - name: Publish
 #       env:
 #         #DONE Replace SecretName with the name of a secret from GitHub https://docs.github.com/en/actions/security-guides/encrypted-secrets#creating-encrypted-secrets-for-an-organization for more info
 #         API_KEY: ${{ secrets.[NUGET_API_KEY] }}
 #
 #       #TODO Replace package_version with an env that grabs the correct package version. May also want to replace RockLib.Collections with an env.
 #       run: dotnet nuget push "RockLib.Collections/bin/Release/RockLib.Collections.[package_version].nupkg" -s "https://api.nuget.org/v3/index.json" -k "$env:API_KEY" 
